# Journey to the Boss

**Topics Covered:** `for` loops, `while True`, `break`, `continue`, `pass`, `for/else`, nested loops, `try/except`, f-strings, `random`  
**Instructions:** Complete all three parts below in order. Each part builds directly on the previous one. Test your program multiple times since random results will be different each run!

> **Note:** This challenge builds on your previous Hero's Encounter program. Have that code nearby as a reference — you will be reusing and improving it!

---

## Part 1: Refactor the Encounter with a Loop

### 📋 Your Task
In your previous program, you copy-pasted the battle round code three times. Now that you know loops, rewrite the battle so the three rounds run automatically using a `for` loop. The logic inside each round stays the same — you are just letting the loop handle the repetition.

### ✅ Requirements

| Requirement | Details |
|---|---|
| Use a `for` loop | Run exactly 3 battle rounds automatically |
| Use `break` | End the battle early if either the hero or monster reaches 0 health before round 3 |
| Use `try/except` | Wrap the hero class input so invalid entries default to Warrior with a friendly message |
| Print round number | Use the loop variable in an f-string to print `--- Round 1 ---`, `--- Round 2 ---` etc. |

### 💡 Hints
- `for round_num in range(1, 4):` will loop 3 times with `round_num` being 1, 2, then 3
- Inside the loop, check after each round if either health is `<= 0` and `break` if so
- For `try/except`, wrap your class input in a `try` block and catch a general `Exception` in the `except` block

### 🖊️ Your Code

```python
# Part 1: Refactor the Encounter with a Loop
# Your Name:
# Date:

import random
import math

# Step 1: Ask for hero name — wrap in try/except to handle unexpected input


# Step 2: Set damage multiplier based on hero class


# Step 3: Set starting health for hero and monster


# Step 4: Print the opening story


# Step 5: Use a for loop to run 3 battle rounds
#         Inside each round: roll attacks, compare, calculate damage, update health, print results
#         Use break if either character reaches 0 health


# Step 6: Print the final outcome based on remaining health

```

### 🧪 Test Your Program

| Test | Hero Class | Did `try/except` work? | Did `break` trigger early? | Who won? |
|---|---|---|---|---|
| 1 | Warrior | | | |
| 2 | Type something invalid | | | |
| 3 | Mage | | | |

> **What happened when you typed an invalid class in Test 2?**
>
> *(paste your output here)*

---

## Part 2: The Adventure Loop

### 📋 Your Task
Now wrap your encounter inside a `while True` loop so the hero keeps adventuring and fighting random monsters until either they have survived **5 encounters** or their health drops to zero. After each encounter, a random event occurs on the road to the next fight.

### ✅ Requirements

| Requirement | Details |
|---|---|
| `while True` loop | Keeps the adventure going encounter after encounter |
| Encounter counter | Track wins using a variable — `break` after 5 victories |
| `break` on death | End the adventure immediately if hero health reaches 0 |
| Random monster | Use `random.randint(1, 4)` and conditionals to pick the monster each encounter |
| Random road event | After each won encounter, use `random.randint(1, 4)` to trigger an event (see table below) |
| `continue` | If the hero wins with full health (no damage taken), skip the road event summary and `continue` to the next encounter immediately |
| `pass` | Use `pass` for the "nothing happens" road event branch |

### 🗺️ Monster Table

| Roll | Monster | Starting Health | Attack Roll |
|---|---|---|---|
| 1 | Goblin | 18 | `random.randint(1, 15)` |
| 2 | Orc | 25 | `random.randint(1, 18)` |
| 3 | Giant Spider | 20 | `random.randint(1, 16)` |
| 4 | Dark Wizard | 30 | `random.randint(1, 20)` |

### 🛤️ Road Events Table

| Roll | Event | Effect |
|---|---|---|
| 1 | Find a health potion | Restore `random.randint(3, 8)` health — print how much was restored |
| 2 | Trip on a tree root | Lose `random.randint(1, 4)` health — print how much was lost |
| 3 | Find a sharpening stone | Boost damage multiplier by 0.2 for the next fight — print the new multiplier |
| 4 | Nothing happens | Use `pass` — print `"The road is quiet... you press on."` |

### 🖥️ Expected Output
Your program should produce output that looks something like this:

```
=========================================
     🗺️  The Hero's Quest Begins!
=========================================
Alex the Warrior sets off into the world...
5 victories stand between you and the Dark Lord's castle.
=========================================

--- Encounter 1 of 5 ---
You venture deeper into the woods...
A Goblin leaps from the shadows!

--- Round 1 ---
...

=========================================
Alex defeats the Goblin! (Wins: 1 / 5)
Alex Health: 17
=========================================

🛤️  On the road...
Alex finds a health potion and restores 5 health!
Alex Health: 22
```

### 💡 Hints
- Set the encounter counter to 0 before the `while True` loop and add 1 each time the hero wins
- Check `if encounter_count >= 5: break` after each victory to end the loop
- Check `if hero_health <= 0: break` immediately after each battle ends
- The road event should only trigger if the hero is still alive after the battle
- Use `continue` to skip the road event if the hero took zero damage that encounter

### 🖊️ Your Code

```python
# Part 2: The Adventure Loop
# Your Name:
# Date:

import random
import math

# Step 1: Ask for hero name and class (keep your try/except from Part 1)


# Step 2: Set damage multiplier and starting health


# Step 3: Print the opening adventure banner


# Step 4: Set encounter counter to 0


# Step 5: Start the while True adventure loop

    # Step 5a: Add 1 to encounter counter and print which encounter this is

    # Step 5b: Randomly select monster using random.randint and conditionals

    # Step 5c: Print the encounter intro with the monster's name

    # Step 5d: Run the 3-round for loop battle (from Part 1)

    # Step 5e: Check if hero is dead — break if so

    # Step 5f: Check if hero won with full health — continue if so

    # Step 5g: Trigger a random road event using random.randint and conditionals

    # Step 5h: Check if 5 encounters are complete — break if so


# Step 6: Print outcome — did the hero survive or fall?

```

### 🧪 Test Your Program

| Test | Did the adventure run for 5 encounters? | Did a road event trigger each time? | Did the hero die at some point? |
|---|---|---|---|
| 1 | | | |
| 2 | | | |
| 3 | | | |

> **Paste one full encounter including the road event from one of your test runs:**
>
> *(paste your output here)*

---

## Part 3: The Boss Battle

### 📋 Your Task
After surviving 5 encounters, the hero arrives at the Dark Lord's castle for the final battle. The boss battle runs for **5 rounds** using a `for/else` loop — if the hero survives all 5 rounds, the `else` block triggers the victory ending. If the hero dies mid-battle, a `break` exits the loop and the `else` is skipped.

### ✅ Requirements

| Requirement | Details |
|---|---|
| `for/else` loop | 5 rounds — `else` block only runs if hero survives all 5 without `break` |
| `break` on death | Exit the loop immediately if hero health reaches 0 mid-battle |
| Boss stats | Dark Lord has 50 health and rolls `random.randint(5, 25)` |
| Dramatic intro | Print a multi-line castle arrival story using f-strings before the battle |
| Final outcome | Victory message in the `else` block, defeat message after the loop if `break` was triggered |

### 🗺️ Boss Stats

| Setting | Value |
|---|---|
| Dark Lord starting health | 50 |
| Dark Lord attack roll | `random.randint(5, 25)` |
| Battle rounds | 5 |

### 🖥️ Expected Output

```
=========================================
     🏰  The Dark Lord's Castle
=========================================
After 5 hard-fought battles, Alex stands
before the gates of the Dark Lord's castle.
The doors creak open. A dark figure emerges...

"So... a Warrior dares challenge me?"

The Dark Lord laughs. The final battle begins!
=========================================

--- Boss Round 1 ---
Alex rolls a 16... The Dark Lord rolls a 11...
Alex strikes! The Dark Lord takes 3 damage.
Alex Health: 17  |  Dark Lord Health: 47

...

=========================================
⚔️  Alex has defeated the Dark Lord!
The kingdom is saved! The Hero's Quest is complete!
=========================================
```

### 💡 Hints
- A `for/else` loop looks like this:
```python
for round_num in range(1, 6):
    # battle logic
    if hero_health <= 0:
        break
else:
    print("You survived all 5 rounds!")
```
- The `else` block only runs if the loop completed without hitting a `break`
- Use a boolean variable `hero_defeated = False` before the loop and set it to `True` inside the `break` condition — check it after the loop to print the defeat message if needed

### 🖊️ Your Code

```python
# Part 3: The Boss Battle
# Your Name:
# Date:

# (This code goes after your Part 2 adventure loop)

# Step 1: Print the castle arrival story using f-strings


# Step 2: Set the Dark Lord's starting health


# Step 3: Set hero_defeated = False before the loop


# Step 4: Run the 5-round for loop boss battle
#         Use break if hero health reaches 0 and set hero_defeated = True
#         Print round number, rolls, damage, and health each round


# Step 5: Use the else block for the victory message


# Step 6: After the loop, check hero_defeated and print the defeat message if True

```

### 🧪 Test Your Program

| Test | Did the hero reach the boss? | Did the `else` victory message trigger? | Did the `break` defeat message trigger? |
|---|---|---|---|
| 1 | | | |
| 2 | | | |
| 3 | | | |

> **Paste your boss battle output from one test run:**
>
> *(paste your output here)*

---

## 🏆 Extension: Loot and Random Events

Expand your adventure by adding a **loot system** that rewards the hero after each victory and builds a small inventory summary at the end.

### 💰 Loot Drops
After defeating each monster, use `random.randint(1, 3)` to determine the loot:

| Roll | Loot | Effect |
|---|---|---|
| 1 | Gold coins | Add `random.randint(5, 20)` coins to a running `gold` total |
| 2 | Health potion | Add 1 to a `potions` counter — hero can carry up to 3 |
| 3 | No loot | Use `pass` — print `"The monster drops nothing of value."` |

### 🧪 Using Potions
If the hero's health drops **below 8** at the end of any round and they have at least 1 potion, automatically use a potion:
- Restore `random.randint(5, 10)` health
- Subtract 1 from `potions`
- Print `f"{hero_name} drinks a health potion and restores {amount} health!"`

### 🗺️ Extended Road Events
Add two new road events to your existing road event table (update your `random.randint` range to `1, 6`):

| Roll | Event | Effect |
|---|---|---|
| 5 | Find a coin purse | Add `random.randint(10, 30)` to `gold` total |
| 6 | Ambushed! | Lose `random.randint(2, 6)` health before the next encounter |

### 🏁 End of Quest Summary
After the final battle (win or lose), print a full quest summary:

```
=========================================
        📜  Quest Summary
=========================================
Hero:      Alex the Warrior
Encounters survived:  5
Gold collected:       47 coins
Potions remaining:    1
Final health:         12
=========================================
```

```python
# Extension: Loot and Random Events
# Your Name:
# Date:

import random
import math

# Step 1: Set up gold = 0 and potions = 0 before the adventure loop


# Step 2: After each monster is defeated, roll for loot and update gold or potions


# Step 3: Inside each battle round, check if hero health is below 8 and potions > 0
#         If so, automatically use a potion


# Step 4: Add the two new road events to your existing road event conditionals


# Step 5: After the final battle, print the quest summary using f-strings

```

---

## 🏁 Reflection Questions

Answer the following questions in your own words:

1. What is the difference between a `for` loop and a `while True` loop? When did you use each one in this challenge?

> *Your answer:*

2. What does `break` do? Give one specific example of where you used it and why.

> *Your answer:*

3. What does `continue` do? How did you use it in the adventure loop?

> *Your answer:*

4. What is a `for/else` loop and why was it a good fit for the boss battle?

> *Your answer:*

5. Where did you use `pass` and why is it useful to have a branch that does nothing?

> *Your answer:*

6. How did `try/except` improve your program compared to the previous challenge?

> *Your answer:*

7. What was the most challenging part of this whole quest and how did you work through it?

> *Your answer:*
